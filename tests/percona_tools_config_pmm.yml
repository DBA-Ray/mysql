---
- hosts: all:!localhost:!proxysql_pro1:!proxysql_pro2
  gather_facts: True
  remote_user: root
  vars:
    percona_tools: True # Whether to install percona tools
    auto_config_pmm: False # Whether to config pmm client to pmm server
  roles:
    - dba_ray.mysql
